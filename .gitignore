# PyInstaller build intermediates — not needed in the repo
build_temp/
*.spec

# Keep dist/ tracked (contains the built EXEs)
# dist/ is intentionally NOT ignored

# Python cache
__pycache__/
*.pyc
*.pyo

# Temporary yt-dlp operations folder
operations/
